import AppLayout from '@/components/layout/app-layout';
import RankTrackerClient from '@/components/rank-tracker/rank-tracker-client';

export default function RankTrackerPage() {
  return (
    <AppLayout>
      <RankTrackerClient />
    </AppLayout>
  );
}
